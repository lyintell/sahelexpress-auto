Pour le backend, nous alons utiliser supabase.
Nous avons deja defini les schema dans DB.md. 

- Crée la structure pour intégrer supabase (dans le dossier ../server, pas dans client), je vais te donner les credentiels après avoir créer le projet dans supabase.

- Cree un fichier DB_SQL.md que je vais copier et coller dans supabase pour crée la base de données.

- Cree un fichier AI_DB_instructions.md dans ../ qui contient toute les instructions pour que je puisse connecter le backend et le frontend, aussi utiliser un domaine (sahelexpress-auto) que j'ai acheter et veut configurer avec cloudflare.
